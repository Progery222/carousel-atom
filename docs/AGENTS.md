<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-06-04 | Updated: 2026-06-04 -->

# docs

## Purpose
Reference documentation for the public REST API (`/api/v1`) exposed by the backend. This is the prose companion to the live OpenAPI/Swagger surface generated by FastAPI.

## Key Files
| File | Description |
|------|-------------|
| `API.md` | Full `/api/v1` spec: `X-API-Key` authentication, all 8 endpoints (`/topics`, `/designs`, `/preview/articles`, `/render`, `/render/edit`, `/render/partial`, `/export/{run_id}.zip`, `/health`), request/response JSON schemas with examples, the unified error envelope, and rate-limit tiers (light 120/min, heavy 30/min). |

## For AI Agents

### Working In This Directory
- `API.md` documents behavior implemented in `backend/api/v1.py` and `backend/api/server.py`. When you change a public route, its auth, its rate-limit tier, or its request/response shape, update `API.md` to match.
- The in-app `/api-docs` page (`frontend/src/pages/ApiDocsPage.tsx`) duplicates much of this content as a hardcoded endpoint list — keep the two in sync when endpoints change.
- Keep examples consistent with the actual `Optional[X]` pydantic schemas in `backend/api/schemas.py`.

### Testing Requirements
- No tests here. Cross-check claims against `backend/tests/test_api_v1.py` and the live OpenAPI schema (`/openapi.json`) when editing.

## Dependencies

### Internal
- Describes `backend/api/` — keep aligned with `v1.py`, `server.py`, `schemas.py`, `auth.py`, `rate_limit.py`.

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->
